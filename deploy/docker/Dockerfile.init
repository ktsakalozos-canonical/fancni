FROM ubuntu:24.04

RUN apt-get update -qq && \
    apt-get install -y -qq --no-install-recommends \
        iptables \
        iproute2 \
        kmod && \
    rm -rf /var/lib/apt/lists/*

COPY deploy/scripts/init-node.sh /init-node.sh
RUN chmod +x /init-node.sh

ENTRYPOINT ["/init-node.sh"]
